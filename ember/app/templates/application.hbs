<header>
  <div class="inner">
    {{link-to "Buddy Buddy" 'application' classNames="buddybuddy-logo" }}

    <nav>
      <ul>
        {{#if session.isAuthenticated}}
        <li>
          {{#link-to  'users.user' currentUserService.user}} Hi, {{currentUserService.user.name}}!{{/link-to}}
        </li>
        <li>
          {{#link-to 'pairings'}}Pairings{{/link-to}}
        </li>
        <li>
          {{#link-to 'application'}}Check In{{/link-to}}
        </li>
        <li>
          <a {{action 'invalidateSession' }}>Sign Out</a>
        </li>
        {{else}}
        {{#link-to 'login'}}Login{{/link-to}}
        {{/if}}

      </ul>
    </nav>
  </div>
</header>


<section class="main">
  <div class="inner">
    {{outlet}}
  </div>
</section>

<footer>
  <div class="inner">
    <p>Made by <a href="http://simplereach.com">SimpleReach</a> in NYC</p>
  </div>
</footer>
